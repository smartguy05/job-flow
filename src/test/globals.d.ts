// Test-only globals seeded by vitest.setup.ts.
declare global {
  // eslint-disable-next-line no-var
  var __testUserId: string;
  // eslint-disable-next-line no-var
  var __testCookie: string;
}

export {};
